export function FounderStory() {
  return (
    <section className="bg-cream py-24">
      <div className="max-w-3xl mx-auto px-6 text-center">
        <p className="text-xs tracking-[0.3em] uppercase text-accent mb-6">Our story</p>
        <blockquote className="font-serif text-2xl md:text-3xl leading-relaxed text-foreground italic">
          “Hängr was born at a quiet café in Stockholm. A friend set her bag on the floor —
          again — and we wondered why something so cared for had nowhere to rest.
          We made the answer: small, weighted, beautifully Nordic.”
        </blockquote>
        <p className="mt-8 text-sm tracking-[0.2em] uppercase text-muted-foreground">
          — The Hängr Atelier
        </p>
      </div>
    </section>
  );
}
